# Практическая работа 4 — Вариант 11 (Avalonia)

Учебный проект: десктопное приложение для расчета трех функций, табулирования значений и построения графика.  
Цель работы: отработка ручного тестирования методом белого ящика.

## Сведения о работе
- Практическая работа: `№4`
- Вариант: `11`
- Разработчик: `Кобелев Д.`
- Учебная группа: `3ИСИП-423`

## Формулы варианта 11
1. Формула страницы 1 (`b`):
`b = y * cbrt(|x|) + cos^3(y) * (|x-y| * (1 + sin^2(z) / sqrt(x+y))) / e^(|x-y| + x/2)`

2. Формула страницы 2 (`a`):
- при `x*y > 0`: `a = (f(x) + y)^2 - sqrt(f(x)*y)`
- при `x*y < 0`: `a = (f(x) + y)^2 + sqrt(|f(x)*y|)`
- при `x*y = 0`: `a = (f(x) + y)^2 + 1`

3. Формула страницы 3 (`y`):
`y = x + sqrt(|x^3 + a - b*e^x|)`

Для страницы 2 пользователь выбирает `f(x)`:
- `sh(x)`
- `x^2`
- `e^x`

## Функциональные возможности
- Страница 1: ввод `x, y, z`, расчет `b`, вывод результата, кнопки `Вычислить`/`Очистить`.
- Страница 2: ввод `x, y`, выбор `f(x)`, расчет `a` по веткам, вывод результата и активной ветки.
- Страница 3: ввод `x0, xk, dx, a, b`, табулирование `y`, вывод таблицы и построение графика.
- Общие функции: навигация по вкладкам, `ToolTip` на элементах UI, подтверждение закрытия приложения.

## Валидация ввода
- Все поля должны содержать корректные числа.
- Для страницы 1: требуется `x + y > 0` (из-за `sqrt(x+y)`).
- Для страницы 3: `dx != 0`.
- Для страницы 3: знак шага должен соответствовать направлению диапазона.
- Для страницы 3: ограничение на число итераций (защита от бесконечного цикла).

## Контрольные тест-кейсы
1. Страница 1 (`b`): `x=1`, `y=-0.5`, `z=0`  
Ожидаемо: `b ≈ -0.36279616514228696`.

2. Страница 2 (`a`, ветка `xy < 0`, `f(x)=x^2`): `x=1`, `y=-0.5`  
Ожидаемо: `a ≈ 0.9571067811865476`.

3. Страница 3 (`y`): `x=0`, `a=26`, `b=-0.3427381222209907`  
Ожидаемо: `y ≈ 5.132517717672389`.

## Скриншоты формул
Ресурсы проекта:
- `Assets/1.png`
- `Assets/2.png`
- `Assets/3.png`

## Технологии
- `C#`
- `.NET 10`
- `Avalonia UI 11`

## Структура проекта
```text
Variant11Avalonia/
├─ Assets/
│  ├─ 1.png
│  ├─ 2.png
│  └─ 3.png
├─ Controls/
│  └─ FunctionChartControl.cs
├─ App.axaml
├─ App.axaml.cs
├─ MainWindow.axaml
├─ MainWindow.axaml.cs
├─ Program.cs
├─ CalculationEngine.cs
├─ Variant11Avalonia.csproj
└─ README.md
```

## Сборка и запуск
```bash
dotnet build
dotnet run
```

## Ручное тестирование (white-box)
1. Проверить пустые/нечисловые значения во всех полях.
2. Проверить три ветки `xy > 0`, `xy < 0`, `xy = 0` на странице 2.
3. Проверить граничные случаи страницы 3: `dx = 0`, неверный знак шага, пустой диапазон.
4. Проверить формирование таблицы `x/y` и построение графика.
5. Проверить кнопки `Очистить` и окно подтверждения выхода.
